import React from 'react';
import SharedEmailNotificationPage from '../../components/admin/SharedEmailNotificationPage';

const AdminEmailNotificationPage: React.FC = () => (
  <SharedEmailNotificationPage
    title="Send Email Notification"
    subtitle="Broadcast announcements or send targeted emails with a live preview before dispatch."
  />
);

export default AdminEmailNotificationPage;
